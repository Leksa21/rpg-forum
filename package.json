{
  "name": "rpg---forum",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "dev": "concurrently -n SERVER,CLIENT -c cyan,magenta \"node --watch server.js\" \"cd client && npm run dev\"",
    "build": "cd client && npm install && npm run build",
    "server": "node --watch server.js",
    "start": "node server.js",
    "test": "jest",
    "test:client": "cd client && npm run test:run",
    "test:all": "jest && cd client && npm run test:run"
  },
  "jest": {
    "testEnvironment": "node",
    "testPathIgnorePatterns": ["/node_modules/", "/client/"]
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.6.3",
    "socket.io": "^4.8.3"
  },
  "devDependencies": {
    "concurrently": "^10.0.3",
    "jest": "^30.4.2"
  }
}
